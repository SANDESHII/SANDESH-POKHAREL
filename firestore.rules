rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /teamStates/{teamId} {
      allow read, write: if true; // In a production app, we'd restrict this, but for this engine it's server-side access mostly
    }
  }
}
